export interface Task {
    id:string;
    taskTitle: string;
    selectedDate: Date | null;
    status: string;
    category: string;
}

export interface Task1 {
    taskTitle: string;
    selectedDate: Date | null;
    status: string;
    category: string;
}
